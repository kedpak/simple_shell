# Linux Shell
## Synopsis
This project is a custom unix shell built from scratch. It is written in C.

## Desription
A unix shell is a command line interpreter. It provides the user with with an interface which allows them to enter commands into the command line. Text scripts are are executed through the terminal emulator.

## Installation

